apiVersion: apps/v1
kind: Deployment
metadata:
  name: az-devops-linux-deploy
  namespace: az-devops
  labels:
    app: az-devops-linux-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: az-devops-linux-deploy
  template:
    metadata:
      labels:
        app: az-devops-linux-deploy
    spec:
      containers:
      - name: az-agent-deploy
        image: local/az-local-agent:v1.5.11
        imagePullPolicy: Never
        env:
          # Needed for mac osx m1 serie
          - name: DOTNET_EnableWriteXorExecute
            value: "0"
          # Manatory settings
          - name: VSTS_AGENT_INPUT_POOL
            value: "Local-kubernetes"
          # Secret  
          - name: VSTS_AGENT_INPUT_URL
            valueFrom:
              secretKeyRef:
                name: azdevops
                key: AZP_URL
          - name: VSTS_AGENT_INPUT_TOKEN
            valueFrom:
              secretKeyRef:
                name: azdevops
                key: AZP_TOKEN
          # Type
          - name: DEF_TYPE
            value: "deploy"
          # Customer
          - name: DEF_CUSTOMER_NAME
            value: "koen"
        volumeMounts:
        - mountPath: /var/run/docker.sock
          name: docker-volume
      volumes:
      - name: docker-volume
        hostPath:
          path: /var/run/docker.sock